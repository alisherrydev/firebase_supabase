import { initializeApp } from 'firebase/app'; import { getFirestore, collection, getDocs, limit, query } from 'firebase/firestore'; const app = initializeApp({ apiKey: 'AIzaSyBTzg28sIYFzTGYHZGyomA2EqUtvsZ_8Ns', projectId: 'customized-shop-webapp' }); const db = getFirestore(app); getDocs(query(collection(db, 'cars'), limit(2))).then(s => { s.forEach(d => console.log(d.data())); process.exit(0); }).catch(e => { console.error(e); process.exit(1); });
