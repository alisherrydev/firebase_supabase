import { initializeApp } from 'firebase/app'; import { getFirestore, collection, getDocs } from 'firebase/firestore'; const app = initializeApp({ apiKey: 'AIzaSyBTzg28sIYFzTGYHZGyomA2EqUtvsZ_8Ns', projectId: 'customized-shop-webapp' }); const db = getFirestore(app); getDocs(collection(db, 'cars')).then(() => { console.log('Read success'); process.exit(0); }).catch(e => { console.error('Read failed:', e.message); process.exit(1); });
