import { initializeApp } from 'firebase/app'; import { getAuth, createUserWithEmailAndPassword } from 'firebase/auth'; import { getFirestore, collection, addDoc, getDocs } from 'firebase/firestore'; const app = initializeApp({ apiKey: 'AIzaSyBTzg28sIYFzTGYHZGyomA2EqUtvsZ_8Ns', projectId: 'customized-shop-webapp' }); const auth = getAuth(app); createUserWithEmailAndPassword(auth, 'testuser123@example.com', 'password123').then((cred) => { console.log('Created user:', cred.user.uid); const db = getFirestore(app); return addDoc(collection(db, 'items'), { name: 'test item' }); }).then(() => { console.log('Successfully wrote to items!'); process.exit(0); }).catch(e => { console.error(e.code, e.message); process.exit(1); });
