import { initializeApp } from 'firebase/app'; import { getAuth, signInWithEmailAndPassword } from 'firebase/auth'; const app = initializeApp({ apiKey: 'AIzaSyBTzg28sIYFzTGYHZGyomA2EqUtvsZ_8Ns', projectId: 'customized-shop-webapp' }); const auth = getAuth(app); signInWithEmailAndPassword(auth, 'test@test.com', 'password123').then(() => { console.log('Email works!'); process.exit(0); }).catch(e => { console.error(e.code, e.message); process.exit(1); });
